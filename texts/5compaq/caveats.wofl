* windows 98 SE
  * don't move mouse(PS2) while disk activity
    \ it stops disc activity
    \ during ftp transfer to it, it will lock up(BSOD) Esception 0E(i think) - if it does, press Enter, and then Ctrl+Alt+Del to sync! (don't ctrl+alt+del from BSOD, it will just lock up - might still sync first, but seemed not to!)
    \

