#!/bin/bash

#this is for when you want to test an update and thus you don't want to also build the docs package and wtw else(1 more package if I remember right) to waste time!

time (./recompile --pkg linux-git && ./inst --pkg linux-git )

