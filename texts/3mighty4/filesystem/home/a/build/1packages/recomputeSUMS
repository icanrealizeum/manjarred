#!/bin/bash

#this is all that's needed to update the sha512() array inside the PKGBUILD in current folder! (note that things will be downloaded if they aren't already!)

#this file is here mainly to let you know that nothing other than running updpkgsums is needed to update the checksums!
#And to avoid accidentally clicking it, it won't be symlinked in each package's folder !

updpkgsums

