Using these scripts from manjaro OS with username z ( location: /home/z/rsync/ folder)
to transfer files with rsync to/from the current manjaro OS into another folder
./manjaro/ then later copy this folder with relaxed permissions(all files
readable by z) so that git can commit them into the manjarred repo.

Scripts also help with preparing a copy of the rsync-ed folder for storage inside git.

