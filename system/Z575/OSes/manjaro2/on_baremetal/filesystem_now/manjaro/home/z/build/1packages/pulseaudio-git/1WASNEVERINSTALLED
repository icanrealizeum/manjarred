Note: don't install this!
if you do, please check that the config files under /etc/pulse/ are in fact not overwritten with a different set of values within, if they are, then ensure PKGBUILD is setting the right values

