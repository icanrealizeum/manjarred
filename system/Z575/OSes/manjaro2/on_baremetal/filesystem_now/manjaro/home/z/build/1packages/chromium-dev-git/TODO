//testing with -Ofast (can't use -flto)
test with -Os (don't forget to also set optimize_for_size = true in args.gn)
----
get updated src/enable_vaapi_on_linux.diff after a while, because some commit removed some *.gyp(i?) and it cannot be applied anymore
-----
